import * as category from './categoryName';

const categories = [
  category.MOVIES,
  category.TRAVEL,
  category.ART,
  category.MUSIC,
  category.DIY,
  category.EXERCISE,
  category.GAMES,
  category.COOKING,
  category.ANIMALS,
  category.ETC,
];

export default categories;
